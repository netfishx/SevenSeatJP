# Indexing blocked while placeholder content remains in production
# (1×1 OG image, 1×1 vehicle photos, undisclosed Tokushoho operator fields).
# Sitemap line intentionally omitted — under Disallow: / it is unreachable
# anyway and only leaks the existence of placeholder pages to crawlers.
# Task 16 restores Allow: / + Sitemap: …/sitemap-index.xml once real
# assets land AND both X-Robots-Tag lines (public/_headers + middleware.ts)
# are deleted in lockstep.
User-agent: *
Disallow: /
